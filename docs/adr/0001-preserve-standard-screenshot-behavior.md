# Preserve standard screenshot behavior

BarCut should prefer importing the macOS-created **Screenshot** rather than recreating it. Preserving **Standard Screenshot Behavior** is more important than receiving the image immediately, so writing the file ourselves is only acceptable when it is indistinguishable from macOS behavior, including the **Floating Thumbnail** and saving to the **Screenshot Destination**.
